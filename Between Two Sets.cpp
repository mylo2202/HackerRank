#include <bits/stdc++.h>

using namespace std;

int main()
{
    int m, n, i, j, betweens = 0;
    cin >> m >> n;
    int a[m], b[n];
    for(i=0; i<m; i++) cin >> a[i];
    for(j=0; j<n; j++) cin >> b[j];
    for(int k = min(a, a + m); k <= max(b, b + n); k++)
    {
        if()
    }

    return 0;
}
